---
const { pathname } = Astro.url;

const navItems = [
  { href: '/', label: '🏠 Inicio', emoji: '🏠' },
  { href: '/secciones/introduccion', label: '📚 Introducción', emoji: '📚' },
  { href: '/secciones/no-informadas', label: '🌐 No Informadas', emoji: '🌐' },
  { href: '/secciones/informadas', label: '🎯 Informadas', emoji: '🎯' },
  { href: '/secciones/local', label: '⛰️ Local', emoji: '⛰️' },
  { href: '/secciones/adversaria', label: '♟️ Adversaria', emoji: '♟️' }
];

const isActive = (href: string) => {
  if (href === '/' && pathname === '/') return true;
  if (href !== '/' && pathname.startsWith(href)) return true;
  return false;
};
---

<nav>
  <ul>
    {navItems.map(item => (
      <li>
        <a
          href={item.href}
          class={isActive(item.href) ? 'nav-link active' : 'nav-link'}
          aria-current={isActive(item.href) ? 'page' : undefined}
        >
          {item.label}
        </a>
      </li>
    ))}
  </ul>
</nav>

<style>
  nav ul {
    display: flex;
    list-style: none;
    gap: 2rem;
  }

  .nav-link {
    color: var(--color-text-muted);
    text-decoration: none;
    font-weight: 500;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    transition: all 0.2s ease;
    position: relative;
  }

  .nav-link:hover {
    color: var(--color-accent);
    background: var(--color-code-bg);
    transform: translateY(-1px);
  }

  .nav-link.active {
    color: var(--color-accent);
    background: var(--color-code-bg);
    font-weight: 600;
  }

  .nav-link.active::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 50%;
    width: 80%;
    height: 2px;
    background: var(--color-accent);
    transform: translateX(-50%);
    border-radius: 1px;
  }

  /* Add subtle animations */
  .nav-link::before {
    content: "";
    position: absolute;
    bottom: 0;
    left: 50%;
    width: 0;
    height: 2px;
    background: var(--color-accent);
    transition: all 0.3s ease;
    transform: translateX(-50%);
  }

  .nav-link:hover::before {
    width: 80%;
  }

  .nav-link.active::before {
    width: 0; /* Hide the hover effect for active links */
  }

  @media (max-width: 768px) {
    nav ul {
      gap: 0.5rem;
      flex-wrap: wrap;
      justify-content: center;
    }

    .nav-link {
      padding: 0.4rem 0.8rem;
      font-size: 0.875rem;
    }
  }
</style>